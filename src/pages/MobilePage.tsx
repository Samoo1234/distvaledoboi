import React from 'react';
import MobileLayout from '../components/mobile/MobileLayout';

/**
 * Página principal para interface mobile (vendedores)
 */
const MobilePage: React.FC = () => {
  return <MobileLayout />;
};

export default MobilePage;
