import React from 'react';
import DesktopLayout from '../components/desktop/DesktopLayout';

/**
 * Página principal para interface de separação (desktop)
 */
const SeparacaoPage: React.FC = () => {
  return <DesktopLayout type="separacao" />;
};

export default SeparacaoPage;
